# The round-trip MOCK editor (offline, no key) needs NOTHING beyond the Python
# stdlib:
#     python run_all_conditions.py --mock --n 20      # mock editor, no credits
#
# The packages below are only needed for Weave tracing + the LIVE round-trip
# path that calls W&B Inference:
openai>=1.30
weave>=0.51
wandb>=0.27.0

# For outputs/frontier.png (run_all_conditions.py). Falls back to frontier.txt
# if unavailable, so it's optional:
matplotlib>=3.7
